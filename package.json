{
  "name": "plane-1513-root",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && yarn install && yarn build",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
